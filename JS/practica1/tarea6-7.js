var contador = 1;

while (contador<=10) {
    if (contador%2 == 0) {
        document.writeln(
            "es par"
        )
    }
    document.writeln(
        contador + "<br>"
    )
    contador++;
}
